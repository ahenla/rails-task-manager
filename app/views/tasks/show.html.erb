<div class="container">
  <h1>Task</h1>
  <h2><%= @task.title %></h2>
  <p><%= @task.details %></p>
  <p><%= @task.completed %></p>
   <%=
      link_to 'delete',
      task_path(@task),
      data: {turbo_method: :delete, turbo_confirm: 'Are you sure'}
    %>
    <div>
      <%=
      link_to 'home', tasks_path%>
    </div>
</div>
